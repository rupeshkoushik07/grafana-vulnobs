apiVersion: 1

# Enable the Vulnobs app in the image, so its pages show up without a manual
# step. It needs no configuration: every feed it reads is public.
apps:
  - type: 'rupesh-vulnobs-app'
    org_id: 1
    disabled: false
